//
//  temp.swift
//  Chat
//
//  Created by Daksh on 23/03/23.
//

import Foundation
import SwiftUI

struct Temp{
    init(){
        /*
        var savedOption:[String: TDataCore] = [:]
        
        savedOption["root"] = DatabaseHelper.shared.saveOption(text: "root")
        
        savedOption["1"] = DatabaseHelper.shared.saveOption(text: "1", parent: savedOption["root"])
        savedOption["2"] = DatabaseHelper.shared.saveOption(text: "2", parent: savedOption["root"])
        var name = 3
        for level in 1..<15{
            for i in 0..<5{
                savedOption["\(name)"] = DatabaseHelper.shared.saveOption(text: "\(name)", parent: savedOption["\(level)"])
                name = name + 1
            }
        }
         */
    }
}
